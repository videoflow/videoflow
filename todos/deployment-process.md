- One thing worth flagging up front, from a previous session: k3s image loading on this machine needs sudo, which has caused cluster deploys to fail even when the GPU setup itself is healthy. If the agent reports image-pull failures, that's the likely culprit rather than anything wrong with the solutions or core.
- Another thing: The deployment process doesn't seem to be completely automated yet. Why can't the videoflow deploy command also build the images so that we test that everything works end to end? The same for videoflow run-local? Why do we have to manually build images before calling these two?
